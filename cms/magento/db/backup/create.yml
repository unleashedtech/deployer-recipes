---
tasks:
    cms:magento:db:backup:create:
        - desc: "Create Magento Backup"
        - run: 'cd {{release_or_current_path}} && {{mage}} setup:backup --db'
        - run: 'cd {{release_or_current_path}} && {{mage}} info:backups:list'
