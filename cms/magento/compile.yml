---
tasks:
  cms:magento:compile:
    - desc: Mage Setup Compile
    - run: cd {{app_path}}
    - run: composer dump-autoload -o
    - run: '{{mage}} setup:di:compile'
    - run: composer dump-autoload -o
